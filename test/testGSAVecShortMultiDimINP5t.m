clear variables; clc;
nd = 3;
nr = 2;
np = 3;
nt = 5;

solf = -9:2;
f = [0 0 0 solf]';
%%
% a = cell(pre, rb, phy, time);
a = cell(1, nr, np, nt);

sol1 = 1:12;

sol2 = -1:10;

a{1, 1, 1, 1} = [0 0 0 sol1]';

a{1, 1, 1, 2} = [0 0 0 sol2]';

a{1, 1, 1, 3} = [zeros(1, 6) sol2(1:9)]';

a{1, 1, 1, 4} = [zeros(1, 9) sol2(1:6)]';

a{1, 1, 1, 5} = [zeros(1, 12) sol2(1:3)]';

sol3 = -2:9;

sol4 = -3:8;

a{1, 2, 1, 1} = [0 0 0 sol3]';

a{1, 2, 1, 2} = [0 0 0 sol4]';

a{1, 2, 1, 3} = [zeros(1, 6) sol4(1:9)]';

a{1, 2, 1, 4} = [zeros(1, 9) sol4(1:6)]';

a{1, 2, 1, 5} = [zeros(1, 12) sol4(1:3)]';

sol5 = -4:7;

sol6 = -5:6;

a{1, 1, 2, 1} = [0 0 0 sol5]';

a{1, 1, 2, 2} = [0 0 0 sol6]';

a{1, 1, 2, 3} = [zeros(1, 6) sol6(1:9)]';

a{1, 1, 2, 4} = [zeros(1, 9) sol6(1:6)]';

a{1, 1, 2, 5} = [zeros(1, 12) sol6(1:3)]';

sol7 = -6:5;

sol8 = -7:4;

a{1, 2, 2, 1} = [0 0 0 sol7]';

a{1, 2, 2, 2} = [0 0 0 sol8]';

a{1, 2, 2, 3} = [zeros(1, 6) sol8(1:9)]';

a{1, 2, 2, 4} = [zeros(1, 9) sol8(1:6)]';

a{1, 2, 2, 5} = [zeros(1, 12) sol8(1:3)]';

sol9 = 2:13;

sol10 = 3:14;

a{1, 1, 3, 1} = [0 0 0 sol9]';

a{1, 1, 3, 2} = [0 0 0 sol10]';

a{1, 1, 3, 3} = [zeros(1, 6) sol10(1:9)]';

a{1, 1, 3, 4} = [zeros(1, 9) sol10(1:6)]';

a{1, 1, 3, 5} = [zeros(1, 12) sol10(1:3)]';

sol11 = 4:15;

sol12 = 5:16;

a{1, 2, 3, 1} = [0 0 0 sol11]';

a{1, 2, 3, 2} = [0 0 0 sol12]';

a{1, 2, 3, 3} = [zeros(1, 6) sol12(1:9)]';

a{1, 2, 3, 4} = [zeros(1, 9) sol12(1:6)]';

a{1, 2, 3, 5} = [zeros(1, 12) sol12(1:3)]';

%%

b = cell(1, nr, np, 2);

b{1, 1, 1, 1} = a{1, 1, 1, 1};

b{1, 1, 1, 2} = a{1, 1, 1, 2};

b{1, 2, 1, 1} = a{1, 2, 1, 1};

b{1, 2, 1, 2} = a{1, 2, 1, 2};

b{1, 1, 2, 1} = a{1, 1, 2, 1};

b{1, 1, 2, 2} = a{1, 1, 2, 2};

b{1, 2, 2, 1} = a{1, 2, 2, 1};

b{1, 2, 2, 2} = a{1, 2, 2, 2};

b{1, 1, 3, 1} = a{1, 1, 3, 1};

b{1, 1, 3, 2} = a{1, 1, 3, 2};

b{1, 2, 3, 1} = a{1, 2, 3, 1};

b{1, 2, 3, 2} = a{1, 2, 3, 2};
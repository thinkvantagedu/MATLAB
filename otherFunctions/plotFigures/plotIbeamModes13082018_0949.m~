clear; clf;
plotData;
% this script plots deformations of modes.
%% part 1:convergence.
cd ~/Desktop/Temp/thesisResults/13082018_0949_Ibeam/3146nodes/trial=1/fixrb;
load('errOriginalIter20Add4.mat', 'errOriginalIter20Add4')
load('node.mat', 'node');
load('elem.mat', 'elem');
% load('errProposedNouiTujIter20Add4.mat', 'errProposedNouiTujIter20Add4')
phiOri = errOriginalIter20Add4.phi.val;

node = node(:, 2:end)';
node([2 3], :) = node([3 2], :);

elem = [elem(:, 2:end)'; zeros(1, length(elem))];

dis = reshape(phiOri(:, 1), [3, 3146]);
dis = dis';
scaleFactor = 1;
pdeplot3D(node, elem, 'ColorMapData', abs(dis(:, 2)));
colormap jet
%%
% caxis(log10([6.5949e-15 0.18891]))
axis image
view([30 0 30])